The .pro file in this folder just builds a version of muscled
that runs as a GUI app (via Qt) rather than as a Terminal application.

All the app does is run its compiled-in muscled code, and show
muscled's output in a Qt QTextEdit widget.
